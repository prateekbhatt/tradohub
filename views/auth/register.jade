extends ../layout

block script
  link(rel='stylesheet', href='/js/lib/bootstrap-fileupload.min.css')
  script(src="/js/lib/bootstrap-fileupload.min.js")

block partial
  .row.form-signin
    .span5
      legend Create your Tradohub account
      include _infoBar

    .span5.well.pull-right(style="background-color:#F0F0F0;")
      h4 Create account
      br
      form(name="registerForm", method="POST", action="/register", enctype="multipart/form-data")

        input.input-medium(type="text", name="name[first]", placeholder="First Name", autofocus)
        input.input-medium.pull-right(type="text", name="name[last]", placeholder="Last Name")
        br

        input.input-block-level(type="email", name="email", placeholder="Email Address")
        br

        input.input-block-level(type="password", name="password", placeholder="Password")
        br

        input.input-block-level(type="text", name="company[name]", placeholder="Company Name")
        br

        select.input-block-level(name="company[country]", required)
          option(value="IND") India

        br
        .fileupload.fileupload-new(data-provides="fileupload")
          P Upload CIN / VAT / Import-Export Document:
            br
            small (for business verification)
          span.btn.btn-file.btn-block
            span.fileupload-new Select file
            span.fileupload-exists Change
            input(type="file", name="imexFile")
          &nbsp;
          span.fileupload-preview
          &nbsp;
          a.close.fileupload-exists(href="#", data-dismiss="fileupload", style="float: none")×

        button.btn.btn-block.btn-large.btn-primary(type="submit")
          span Create Account
        br
        small By creating an account you agree to our 
          a(href="/about/terms") Terms 
          span and 
          a(href="/about/privacy") Privacy Policy